# report_pipeline 의존성
# 설치: pip install -r requirements.txt

# HTTP 요청 (LM Studio API)
requests>=2.31.0
urllib3>=2.0.0

# Apple Silicon 음성 전사 (mlx-whisper)
# macOS + Apple Silicon 전용; Intel Mac에서는 설치 불필요
mlx-whisper>=0.4.0

# WebVTT 자막 파싱
webvtt-py>=0.4.6

# 외부 CLI 도구 (pip 외부 — 별도 설치 필요)
# yt-dlp   → pip install yt-dlp  or  brew install yt-dlp
# ffmpeg   → brew install ffmpeg
